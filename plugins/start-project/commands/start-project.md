---
description: Plan a new project from vision through scaffold (7 gated phases)
argument-hint: [slug]
---

Invoke the `start-project` skill from this plugin (use the Skill tool with
skill name `start-project:start-project`), passing through the arguments:

$ARGUMENTS

If no argument was given, pass nothing — the skill handles asking for a slug
and detecting resume mode.
